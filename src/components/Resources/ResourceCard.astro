---
import { Image } from "astro:assets";

const { title, urlResource, imageResource, description} = Astro.props

// const response = await fetch(urlResource)
// const data = await response.text()

---
<article class="
	 w-[360px] border-b-2 
	 border-stone-300 hover:border-stone-400 dark:border-zinc-800 dark:hover:border-zinc-700
">
	<a href={urlResource} target="_blank" class="flex flex-row items-center gap-2">
		{
			imageResource ?
			<Image
				src={imageResource}
				alt="resource preview"
				width={36}
				height={36}
			/> :
			<p class="text-3xl">📁</p>
		}
		<div class="py-4">
			<h2 class="text-lg md:text-xl">{title}</h2>
			<p class="text-xs">{description}</p>
		</div>
	</a>
</article>