---
import { Image } from "astro:assets";

const { title, urlResource, imageResource, description} = Astro.props

// const response = await fetch(urlResource)
// const data = await response.text()

---
<article class="
	 w-[360px] border-b-2 
	 border-slate-300 hover:border-slate-400 dark:border-slate-800 dark:hover:border-slate-700 transition duration-300
">
	<a href={urlResource} target="_blank" class="flex flex-row items-center gap-2">
		{
			imageResource ?
			<Image
				src={imageResource}
				alt="resource preview"
				width={36}
				height={36}
			/> :
			<p class="text-3xl">📁</p>
		}
		<div class="py-4">
			<h2 class="text-lg md:text-xl">{title}</h2>
			<p class="text-xs">{description}</p>
		</div>
	</a>
</article>